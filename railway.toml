[build]
# Railpack'e backend/ dizinini proje kökü olarak göster
# Böylece requirements.txt doğru okunur ve Procfile algılanır
rootDirectory = "backend"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
